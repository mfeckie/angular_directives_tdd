'use strict';

angular.module('mfApp')
  .controller('MainCtrl', function ($scope) {
    $scope.stuff = [
      'Some',
      'Words',
      'To',
      'Render',
  	  'On',
  	  'The',
  	  'Page'
    ];
  });
